{
  "name": "basic-quote-api",
  "version": "1.0.0",
  "description": "A basic daily quote API written in TypeScript with no external libraries",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^18.11.18",
    "jest": "^29.7.0",
    "ts-jest": "^29.3.4",
    "typescript": "^4.9.4"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": [
      "**/__test__/**/*.test.ts"
    ]
  }
}
